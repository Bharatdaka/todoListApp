export class Task {
    id: number;
    label: string;
    priority: number;
    status: boolean;
    
    constructor(label, priority, status) {
        this.id ;
        this.label = label;
        this.priority = priority;
        this.status = status;
      
    }
}